using System;

namespace ConsoleApp3
{
    class Program
    {
        static void Main(string[] args)
        {
            //creates an object that picks a random number from 0-1000
            //starts loop that will contiue as long as the user guess is wrong
            //prompts user for a number between 0-1000
            //saves user's number
            //checks if the number the user inputed is the same as the random number
                //if it is the same, the loop ends and the computer tells the user they are correct
            //if the inputed number is higher, the computer will write 'lower' and restarts loop
            //if the inpured number is lower, the computer will write 'lower' and restarts loop
        }
    }
}
